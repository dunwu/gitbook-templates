{
	"name": "gitbook-api-templates",
	"scripts": {
		"clean": "rimraf _book",
		"prebuild": "gitbook install",
		"build": "npm run clean & gitbook build ./ --log=debug --debug",
		"start": "gitbook serve"
	},
	"dependencies": {
		"gitbook-plugin-theme-api": "^1.1.2"
	},
	"devDependencies": {
		"gh-pages": "^2.1.1",
		"rimraf": "^3.0.0"
	}
}
