{
  "name": "gitbook-notes",
  "version": "1.0.0",
  "description": "gitbook-notes",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf _book",
    "prepare": "gitbook install",
    "build": "npm run prepare && gitbook build ./ --log=debug --debug",
    "start": "npm run build && gitbook serve",
    "publish": "npm run clean && npm start"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:atlantis1024/gitbook-notes.git"
  },
  "keywords": [
    "gitbook",
    "gitlab-ci"
  ],
  "author": "victor zhang",
  "license": "ISC",
  "devDependencies": {
    "rimraf": "^2.6.1"
  }
}
